# Buf v2 workspace configuration
# Run `buf lint` and `buf generate` from the proto/ directory.
version: v2

modules:
  - path: .

lint:
  use:
    - STANDARD
  except:
    - FIELD_NOT_REQUIRED   # allow optional fields without explicit presence

breaking:
  use:
    - FILE
